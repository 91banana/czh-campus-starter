@Entry
@Component
struct LostFoundDetailPage {
  build() {
    Column() {
      Text('失物详情 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
