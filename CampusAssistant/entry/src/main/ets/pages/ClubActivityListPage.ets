@Entry
@Component
struct ClubActivityListPage {
  build() {
    Column() {
      Text('社团活动列表 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
