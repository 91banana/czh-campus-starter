@Entry
@Component
struct MyFavoritesPage {
  build() {
    Column() {
      Text('我的收藏 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
