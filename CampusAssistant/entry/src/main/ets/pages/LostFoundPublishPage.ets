@Entry
@Component
struct LostFoundPublishPage {
  build() {
    Column() {
      Text('发布失物 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
