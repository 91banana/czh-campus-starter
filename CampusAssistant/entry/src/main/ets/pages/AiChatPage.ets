@Entry
@Component
struct AiChatPage {
  build() {
    Column() {
      Text('AI对话 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
