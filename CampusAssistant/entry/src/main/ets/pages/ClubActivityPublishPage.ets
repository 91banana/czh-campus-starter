@Entry
@Component
struct ClubActivityPublishPage {
  build() {
    Column() {
      Text('发布活动 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
