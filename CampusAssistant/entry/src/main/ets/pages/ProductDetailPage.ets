@Entry
@Component
struct ProductDetailPage {
  build() {
    Column() {
      Text('商品详情 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
