@Entry
@Component
struct MyLostFoundPage {
  build() {
    Column() {
      Text('我的失物 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
