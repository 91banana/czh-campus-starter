@Entry
@Component
struct RoomSchedulePage {
  build() {
    Column() {
      Text('教室课表 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
