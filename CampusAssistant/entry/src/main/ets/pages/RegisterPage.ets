@Entry
@Component
struct RegisterPage {
  build() {
    Column() {
      Text('注册 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
