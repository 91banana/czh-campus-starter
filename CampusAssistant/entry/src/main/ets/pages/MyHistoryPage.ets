@Entry
@Component
struct MyHistoryPage {
  build() {
    Column() {
      Text('浏览历史 - 待实现')
        .fontSize(20)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
